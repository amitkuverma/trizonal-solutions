import * as React from "react";
import { Layout } from "../layout/layout";

export const About = () => {
    return (
        <Layout active={"about"}>
            <div>About</div>
        </Layout>
    )
}
