import * as React from "react";
import { Layout } from "../layout/layout";

export const About = () => {
    return (
        <Layout active={"about"}>
             <h1 className="common-header p-4 text-white fnt-lg-42">About</h1>
        </Layout>
    )
}
