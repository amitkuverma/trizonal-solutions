import * as React from "react";
import { Layout } from "../layout/layout";

export const VerdorClaimServices = () => {
    return (
        <Layout active={"vendor-claim-service"}>
            <h1>hello</h1>
        </Layout>
    )
}