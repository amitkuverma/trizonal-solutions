import * as React from "react";
import { Layout } from "../layout/layout";

export const Contact = () => {
    return (
        <Layout active={"contact"}>
            <div>Contact</div>
        </Layout>
    )
}
